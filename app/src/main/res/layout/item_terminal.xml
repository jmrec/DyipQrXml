<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:cardCornerRadius="8dp"
    app:cardElevation="2dp"
    app:cardBackgroundColor="?attr/colorSurface"
    android:layout_margin="8dp">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <LinearLayout
            android:id="@+id/layoutText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:layout_toStartOf="@+id/buttonFavorite"
            android:layout_toLeftOf="@+id/buttonFavorite"
            android:layout_alignParentStart="true"
            android:layout_alignParentLeft="true"
            android:layout_marginEnd="48dp">

            <!-- Route Code -->
            <TextView
                android:id="@+id/textTerminalName"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Route Code"
                android:textColor="?attr/colorOnSurface"
                android:textSize="18sp"
                android:textStyle="bold"
                android:layout_marginBottom="4dp" />

            <!-- Route Description (Start → End) -->
            <TextView
                android:id="@+id/textTerminalDescription"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Start → End Terminal"
                android:textColor="?attr/colorOnSurfaceVariant"
                android:textSize="14sp"
                android:layout_marginBottom="8dp" />

            <!-- Route Info (Fare and Travel Time) -->
            <TextView
                android:id="@+id/textLocationInfo"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Fare: ₱0.00 • 0 min"
                android:textColor="?attr/colorPrimary"
                android:textSize="12sp"
                android:drawablePadding="4dp" />
        </LinearLayout>

        <!-- Favorite Toggle Button -->
        <com.google.android.material.imageview.ShapeableImageView
            android:id="@+id/buttonFavorite"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_alignParentEnd="true"
            android:contentDescription="Toggle favorite"
            android:clickable="true"
            android:focusable="true"
            android:padding="4dp"
            android:src="@drawable/ic_bookmark_add"
            android:tint="?attr/colorPrimary" />
    </RelativeLayout>

</com.google.android.material.card.MaterialCardView>